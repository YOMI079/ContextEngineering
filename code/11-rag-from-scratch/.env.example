# Only the ONLINE path (embeddings, rerank, generation) needs keys.
# The offline core (chunk / bm25 / fuse / pack) and the whole test suite run
# with none of these set.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
VOYAGE_API_KEY=
COHERE_API_KEY=
