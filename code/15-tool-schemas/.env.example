# The offline core (token-budget calculator + schema validation) needs NO keys.
# A key is only needed if you extend the demo to call provider-native structured
# output via the optional 'api' extra (see README). Fill in whichever you use.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
